{
    "layer": "top",
    "position": "top",
    "height": 30,
    "modules-left": [
        "hyprland/workspaces"
    ],
    "modules-center": [
        "clock"
    ],
    "modules-right": [
        "network",
        "pulseaudio",
        "battery"
    ],
    "hyprland/workspaces": {
        "on-click": "activate",
        "on-scroll-up": "hyprctl dispatch workspace e+1",
        "on-scroll-down": "hyprctl dispatch workspace e-1",
        "all-outputs": true,
        "format": "{name}"
    },
    "clock": {
        "format": "{:%H:%M %d/%m}",
        "tooltip-format": "<tt>{calendar}</tt>\n<big>{:%Y %B}</big>\n:: Week {:%V}",
        "format-alt": "{:%d/%m}",
        "on-click": "exec [ -x \"$(command -v gnome-calendar)\" ] && gnome-calendar || [ -x \"$(command -v korganizer)\" ] && korganizer || notify-send 'Calendar' 'Calendar application not found'"
    },
    "network": {
        "format-wifi": "{essid} ({signalStrength}%)",
        "format-ethernet": "Ethernet ({ifname})",
        "tooltip-format": "{ifname}: {ipaddr}",
        "on-click": "exec [ -x \"$(command -v nm-connection-editor)\" ] && nm-connection-editor || [ -x \"$(command -v nmtui)\" ] && nmtui || notify-send 'Network Manager' 'Network manager not available'"
    },
    "pulseaudio": {
        "scroll-step": 5,
        "format": "{icon} {volume}% {format_source}",
        "format-bluetooth": "{icon} {volume}% {format_source} (bluetooth)",
        "format-muted": "婢 muted",
        "format-icons": {
            "headphone": "🎧",
            "hands-free": "☏",
            "headset": "🎧",
            "phone": "📱",
            "portable": "📱",
            "car": "🚗",
            "default": ["🔈", "🔉", "🔊"]
        },
        "on-click": "exec [ -x \"$(command -v pavucontrol)\" ] && pavucontrol || [ -x \"$(command -v alsamixer)\" ] && alsamixer || notify-send 'Audio Control' 'Audio mixer not available'"
    },
    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{capacity}% {icon}",
        "format-charging": "{capacity}% ",
        "format-plugged": "{capacity}% ",
        "format-alt": "{time} {icon}",
        "format-icons": ["", "", "", "", ""],
        "on-click": "exec [ -x \"$(command -v systemctl)\" ] && systemctl suspend || [ -x \"$(command -v loginctl)\" ] && loginctl suspend || notify-send 'Power Management' 'Suspend not available'"
    }
}